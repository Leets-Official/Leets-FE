'use client';

import { styled } from 'styled-components';
import { MQ } from '@/constants';

export const TimelineImageContainer = styled.div`
  ${MQ({
    width: [20.5, 20.5, 29.5, 36.0],
    height: [556.2, 556.9, 801.4, 978.0],
  })}
`;

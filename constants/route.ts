export const ADMIN = {
  HOME: '/admin/application',
  LOGIN: '/admin',
} as const;

export const USER = {
  HOME: '/',
  LOGIN: '/login',
  APPLY: '/apply',
  CERTIFICATE: '/certificate',
} as const;
